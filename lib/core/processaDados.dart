// Aqui é onde será feito todo o processamento dos dados

processaDadosLogin(var email, var senha) {
  //Verifica se existe no banco de dados
  //se sim, manda para página Biblioteca
  //se não, retorna um erro
}

processaDadosCadastro(var nome, var email, var telefone, var senha) {
  //Verifica se já existe
  //se sim, retorna um erro
  //se não, adiciona no banco de dados e manda para Biblioteca
}
